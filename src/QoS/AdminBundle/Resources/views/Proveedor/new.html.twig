{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Proveedor creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('Proveedor') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
